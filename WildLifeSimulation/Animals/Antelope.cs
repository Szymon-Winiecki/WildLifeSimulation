using WildLifeSimulation.World;

namespace WildLifeSimulation.Animals
{
    class Antelope : Animal
    {
        public Antelope(Map map, Gender gender, Position position, Reporter reporter) : base(map, gender, position, reporter) { }
}
}
